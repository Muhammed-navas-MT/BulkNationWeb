<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Wallet</title>
    <!-- New CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }
        .navbar {
            background-color: #ff8c00;
        }
        .navbar-brand, .nav-link {
            color: white !important;
        }
        .nav-link:hover, .nav-link.active {
            color: #ffd700 !important;
        }
        .card {
            border: none;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
        .btn-primary {
            background-color: #ff8c00;
            border-color: #ff8c00;
        }
        .btn-primary:hover {
            background-color: #e67e00;
            border-color: #e67e00;
        }
        .table th {
            background-color: #ff8c00;
            color: white;
        }
        .badge {
            font-size: 0.8em;
            padding: 0.5em 0.7em;
        }
        .wallet-section {
            max-width: 900px;
            margin: 50px auto;
        }
        .wallet-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .wallet-balance {
            color: orange;
        }
        .transaction-table {
            margin-top: 20px;
        }
        .transaction-table thead th {
            color: orange;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">Bulk Nation</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/userProfile"><i class="fas fa-user"></i> My Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/orderDetails"><i class="fas fa-box"></i> My Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/userAdresses"><i class="fas fa-map-marker-alt"></i> My Addresses</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/wallet"><i class="fas fa-wallet"></i> BN Cash</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="fas fa-credit-card"></i> Saved Cards</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Wallet Section -->
    <div class="container wallet-section">
        <div class="wallet-header">
            <div>
                <h2 class="wallet-balance">Wallet Balance</h2>
                <p class="text-4xl fw-bold">₹<%= wallet.balance ||0 %></p>
            </div>
            <!-- <button class="btn btn-success">Add Money</button> -->
        </div>
        <%if(wallet.length==0){%>
            <br>
            <br>
            <div class="text-center">
                <img src="assets/imgs/banner/wallet.png" alt="No Transactions" style="max-width: 200px;">
                <h3 class="mt-3">You don't have any BN Cash yet.</h3>
                <p>You earn BN Cash every time you shop on BN. You can use it to purchase products from BN in the future.</p>
                <a href="/shopPage" class="btn btn-warning">Explore BN</a>
            </div>
            <% }else{%>
        <div class="transaction-table">
            <table class="table table-striped">
                <thead>
                    <tr style="color: white;">
                        <th style="color: white;">Date</th>
                        <th style="color: white;">Transaction Type</th>
                        <th style="color: white;">Amount</th>
                        <th style="color: white;">Status</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let i = 0; i < wallet.transactions.length; i++) { %>
                        <tr>
                            <td><%= wallet.transactions[i].date.toLocaleDateString() %></td>
                            <td><%= wallet.transactions[i].type %></td>
                            <td class="text-success">₹<%= wallet.transactions[i].amount %></td>
                            <td class="text-success"><%= wallet.transactions[i].status %></td>
                        </tr>
                    <% } %>
               <%}%>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
