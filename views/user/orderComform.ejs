<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #FFB534;
            --secondary-color: #FBF6EE;
        }
        body {
            background-color: var(--secondary-color);
        }
        .order-success-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .order-success-icon {
            font-size: 5rem;
            color: var(--primary-color);
        }
        .order-details {
            background-color: rgba(255, 181, 52, 0.1);
            border-radius: 10px;
        }
        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        .btn-primary:hover, .btn-primary:focus {
            background-color: #e6a02f;
            border-color: #e6a02f;
        }
        .btn-outline-primary {
            color: var(--primary-color);
            border-color: var(--primary-color);
        }
        .btn-outline-primary:hover, .btn-outline-primary:focus {
            background-color: var(--primary-color);
            color: var(--secondary-color);
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card order-success-card">
                    <div class="card-body text-center p-5">
                        <i class="fas fa-check-circle order-success-icon mb-4"></i>
                        <h1 class="card-title mb-4">Order Successful!</h1>
                        <p class="lead mb-5">Thank you for your purchase. Your order has been processed successfully.</p>
                        
                        <div class="order-details p-4 mb-4">
                            <h5 class="mb-4">Order Details</h5>
                            <div class="row mb-3">
                                <!-- <div class="col-6 text-start">
                                    <i class="fas fa-receipt me-2"></i>Order ID
                                </div>
                                <div class="col-6 text-end fw-bold">#ORD-12345</div> -->
                            </div>
                            <div class="row mb-3">
                                <div class="col-6 text-start">
                                    <i class="fas fa-calendar-alt me-2"></i>Order Date
                                </div>
                                <div class="col-6 text-end"><%= date%></div>
                            </div>
                            <div class="row">
                                <div class="col-6 text-start">
                                    <i class="fas fa-money-bill-wave me-2"></i>Total Amount
                                </div>
                                <div class="col-6 text-end fw-bold">₹<%=totalPrice%></div>
                            </div>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <a href="/orderDetails" class="btn btn-primary btn-lg">View Order Details</a>
                            <a href="/shopPage" class="btn btn-outline-primary btn-lg">Continue Shopping</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>